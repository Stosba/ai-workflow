[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "convert"
version = "0.1.0"

[project.scripts]
convert = "convert.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
